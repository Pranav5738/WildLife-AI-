
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

spring.datasource.url=jdbc:mysql://nozomi.proxy.rlwy.net:21333/railway?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=QZgXPjD0FEFLEIjbJixWiyZCGiiYKuz
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.application.name=wildlife

# Server port (Render provides PORT automatically)
server.port=${PORT:8082}

# ========================
# JWT CONFIG
# ========================
jwt.secret=${JWT_SECRET}
jwt.expiration-ms=86400000

# ========================
# FILE UPLOAD
# ========================
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# ========================
# DATABASE (MYSQL / LOCAL / RAILWAY)
# ========================
spring.datasource.url=${MYSQL_URL}
spring.datasource.username=${MYSQLUSER}
spring.datasource.password=${MYSQLPASSWORD}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# ========================
# TWILIO
# ========================
twilio.account.sid=${TWILIO_ACCOUNT_SID}
twilio.auth.token=${TWILIO_AUTH_TOKEN}
twilio.phone.number=${TWILIO_PHONE_NUMBER}

user.phone.number=${USER_PHONE_NUMBER}

# ========================
# AWS S3
# ========================
aws.accessKey=${AWS_ACCESS_KEY}
aws.secretKey=${AWS_SECRET_KEY}
aws.region=${AWS_REGION}
aws.bucketName=${AWS_BUCKET_NAME}
